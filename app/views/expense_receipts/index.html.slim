.container
  .col-md-10.offset-md-1.panel.panel-default
    .panel-body
      .page-header
        h1 Expense Receipts
        = link_to(fa_icon("file-text-o", text: "New Expense Receipt"), new_expense_receipt_path, class: "btn indigo")
        #all-expense_receipts
          - cache 'expense_receipts' do
            = render @expense_receipts, cached: true
= will_paginate(@expense_receipts)
