$('.modal').each(function(index, element) {
  $(element).modal('hide');
});
$('#<%= @contact.id %>').replaceWith('<%= j render @contact %>');
<%= add_gritter("Contact was successfully updated.", title: "Retail Merge", sticky: false, image: :success) %>