$('.modal').each(function(index, element) {
  $(element).modal('hide');
});
$('#<%= @adjustment.id %>').replaceWith('<%= j render @adjustment %>');
<%= add_gritter("Adjustment was successfully updated.", title: "Retail Merge", sticky: false, image: :success) %>