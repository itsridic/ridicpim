<tr>
  <td><%= order_item.product.name %></td>
  <td><%= order_item.quantity %></td>
  <td><%= number_to_currency(order_item.cost) %></td>
  <td><%= number_to_currency(order_item.average_cost) %></td>
</tr>