$('.modal').each(function(index, element) {
  $(element).modal('hide');
});
$('#<%= @product.id %>').replaceWith('<%= j render @product %>');
<%= add_gritter("Product was successfully updated.", title: "Retail Merge", sticky: false, image: :success) %>