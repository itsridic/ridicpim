.container
  .col-md-10.col-md-offset-1.panel.panel-default
    .panel-body.new-sales-receipt-form
      h1 Edit Sales Receipt
      = render 'form'
      = link_to 'Back', sales_receipts_path

javascript:
  $(".choose-product").each(function() {
    if ($(this).val()) {
      
    } else {
      $(this).hide();
    }
  })